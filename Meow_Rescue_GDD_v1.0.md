# Meow Rescue — Game Design Document (GDD) v5.0

> **슬라이딩 블록 퍼즐 게임. 블록을 밀어서 갇힌 고양이를 탈출시키세요!**

---

## 1. 게임 개요 (Game Overview)

| 항목 | 내용 |
|------|------|
| **게임명** | Meow Rescue |
| **장르** | 슬라이딩 블록 퍼즐 (Rush Hour / Unblock Me 스타일) |
| **플랫폼** | Android (Kotlin Native) |
| **타겟 유저** | 전연령, 캐주얼 퍼즐 게임 애호가 |
| **수익 모델** | 광고 기반 (AdMob 배너/전면/보상형 광고) |
| **핵심 한 줄 요약** | 격자판 위의 블록을 밀어서 고양이 블록이 출구로 나갈 수 있도록 길을 만드는 슬라이딩 퍼즐 게임 |

### 1.1 게임 컨셉

플레이어는 격자판 위에 놓인 블록들을 드래그하여 이동시킵니다. 가로 블록은 좌우로, 세로 블록은 상하로만 이동 가능합니다(1칸짜리 블록은 상하좌우 모두 이동 가능). 고양이 블록이 출구까지 도달하면 스테이지 클리어입니다.

모든 스테이지는 시드 기반으로 자동 생성되며, BFS 솔버가 풀이 가능성과 최적 이동 횟수를 검증합니다. 품질 필터가 단조로운 퍼즐을 걸러내어 다양하고 재미있는 퍼즐만 제공합니다.

### 1.2 게임의 차별점

- **다방향 출구**: 고양이가 RIGHT, LEFT, TOP, BOTTOM 네 방향으로 탈출 가능
- **5가지 신규 메커니즘**: 벽 블록, 얼음 타일, 연결 블록, 포탈, 멀티캣으로 퍼즐 다양성 확보
- **품질 필터**: 단일 방향/단일 블록/독립 이동 퍼즐을 자동 필터링하여 품질 보장
- **고양이 컬렉션**: 20마리 고양이를 스테이지 클리어로 해금하고, 게임에서 사용
- **탈출 애니메이션**: 벽 열림 → 고양이 슬라이드 → 파티클 이펙트의 연출
- **절차적 생성 + BFS 검증**: 모든 스테이지가 풀이 가능하며 최적 해가 보장됨
- **스냅 애니메이션**: 블록 이동 시 부드러운 ease-out 스냅 효과

---

## 2. 핵심 게임플레이 (Core Gameplay)

### 2.1 기본 메커니즘

#### 2.1.1 블록 이동

- **격자**: 5x5 ~ 7x7 크기 (스테이지에 따라 증가)
- **블록 종류**: 고양이 블록(1칸), 열쇠 블록(1칸, 금색), 일반 블록(1~3칸, 가로/세로), 벽 블록(1칸, 고정), 연결 블록(쌍으로 동시 이동)
- **이동 규칙**:
  - 가로 블록: 좌우로만 이동
  - 세로 블록: 상하로만 이동
  - **1칸 블록: 상하좌우 모두 이동 가능** (양방향 이동)
- **조작**: 블록을 터치하여 드래그, 놓으면 가장 가까운 칸으로 스냅

#### 2.1.2 출구 방향

스테이지마다 고양이의 탈출 방향이 다릅니다:

| 출구 방향 | 고양이 블록 | 설명 |
|----------|-----------|------|
| **RIGHT** | 1칸 | 오른쪽 벽으로 탈출 |
| **LEFT** | 1칸 | 왼쪽 벽으로 탈출 |
| **TOP** | 1칸 | 위쪽 벽으로 탈출 |
| **BOTTOM** | 1칸 | 아래쪽 벽으로 탈출 |

고양이 블록이 1x1이므로 출구와 다른 행/열에서 시작하여 **경로 탐색** 퍼즐이 가능합니다. 출구 방향은 시드 기반으로 결정되며, 100개 스테이지에서 네 방향이 골고루 등장합니다.

#### 2.1.3 열쇠-자물쇠 메커니즘 (스테이지 16+)

| 요소 | 설명 |
|------|------|
| **열쇠 블록** | 금색 1x1 블록, 드래그로 이동 가능 |
| **자물쇠 셀** | 출구 옆 고정 위치, 금색 테두리 표시 |
| **작동 방식** | 열쇠 블록을 자물쇠 셀 위로 이동해야 출구가 열림 |
| **출구 표시** | 열쇠 미배치 시 출구 화살표가 흐리게 표시 (alpha 80) |
| **오버레이** | 블록에 가려져도 자물쇠 셀 우측 상단에 🔒 뱃지 항상 표시 |

#### 2.1.4 체크포인트 메커니즘 (스테이지 31+)

| 요소 | 설명 |
|------|------|
| **체크포인트 셀** | 별(⭐) 마커가 표시된 셀 |
| **배치 규칙** | 고양이~출구 직선 경로에서 벗어난 위치 (우회 강제) |
| **작동 방식** | 고양이가 체크포인트를 경유(pass-through 포함)해야 탈출 가능 |
| **시각 피드백** | 미도달=회색 원형 배경 + 펄스 링, 도달=금색 전환 + 400ms 펄스 링 |
| **오버레이** | 블록에 가려져도 좌측 상단에 ⭐/✓ 뱃지 항상 표시 |

#### 2.1.5 힌트 배너

HUD 아래, 보드 위에 조건 안내 텍스트를 표시합니다:

| 조건 | 힌트 텍스트 |
|------|------------|
| 열쇠 미배치 | "🔑→🔒 열쇠를 자물쇠 칸에 놓으세요" |
| 체크포인트 미경유 | "⭐ 별을 먼저 지나가세요" |
| 체크포인트 도달 | "⭐ ✓" |
| 벽 블록 등장 | "🧱 갈색 블록은 고정 장애물입니다" |
| 얼음 타일 등장 | "❄ 얼음 위에서 블록이 멈추지 않습니다" |
| 연결 블록 등장 | "🔗 연결된 블록은 함께 움직입니다" |
| 포탈 등장 | "🌀 고양이가 포탈에 들어가면 반대편으로 이동" |
| 멀티캣 등장 | "🐱🐱 모든 고양이를 탈출시키세요" |
| 조건 모두 충족 | 배너 자동 숨김 |

#### 2.1.6 스테이지별 피처 적용

| 구간 | 피처 |
|------|------|
| **1~15** | 1x1 고양이만 (새 이동 방식 학습) |
| **16~30** | 열쇠 50% 확률 등장 |
| **31~50** | 열쇠 또는 체크포인트 (50/50, 상호 배타) |
| **51~70** | 벽 블록 1~3개 + 열쇠/체크포인트 |
| **71~90** | 얼음 타일 2~4셀 + 벽 + 열쇠/체크포인트 |
| **91~110** | 연결 블록 1쌍 + 얼음/벽 + 열쇠/체크포인트 |
| **111~130** | 포탈 1쌍 + 연결/얼음/벽 + 열쇠/체크포인트 |
| **131+** | 멀티캣 30% + 모든 메커니즘 |

#### 2.1.7 벽 블록 (스테이지 51+)

| 요소 | 설명 |
|------|------|
| **외형** | 어두운 갈색(#5D4037) + X 패턴 |
| **특성** | 이동 불가, 드래그 무시 |
| **배치** | 1~3개, 빈 셀에 랜덤 배치 |
| **효과** | 정적 장애물로 퍼즐 경로 제약 추가 |

#### 2.1.8 얼음 타일 (스테이지 71+)

| 요소 | 설명 |
|------|------|
| **외형** | 하늘색(#81D4FA) 반투명 배경 + ❄ 아이콘 |
| **특성** | 블록이 얼음 위에서 멈출 수 없음 |
| **작동 방식** | 블록이 얼음 셀에 도달하면 벽/다른 블록에 부딪힐 때까지 계속 슬라이딩 |
| **배치** | 2~4셀, 고양이 시작 위치 제외 |

#### 2.1.9 연결 블록 (스테이지 91+)

| 요소 | 설명 |
|------|------|
| **외형** | 주황색(#FF6F00) + 🔗 체인 아이콘 |
| **특성** | 같은 linkId를 가진 2개 블록이 한 쌍 |
| **작동 방식** | 한쪽을 드래그하면 파트너도 같은 방향·거리로 동시 이동 |
| **제약** | 양쪽 모두 이동 가능해야 드래그 허용 |
| **Undo** | 양쪽 블록 모두 원래 위치로 복원 |

#### 2.1.10 포탈 (스테이지 111+)

| 요소 | 설명 |
|------|------|
| **외형** | A=보라색(#7C4DFF), B=청록색(#00BFA5) 맥동 원형 |
| **대상** | 고양이 블록만 텔레포트 (일반 블록 무시) |
| **작동 방식** | 고양이가 포탈 A에 도착하면 B로 즉시 워프 (역방향도 가능) |
| **목적지 검사** | 워프 대상 셀이 비어있어야 텔레포트 발동 |
| **Undo** | 워프 전 위치로 복원 후 이동 되돌림 |

#### 2.1.11 멀티캣 (스테이지 131+)

| 요소 | 설명 |
|------|------|
| **2번째 출구** | 파란색(#42A5F5) 화살표, 1번째 출구와 다른 방향 |
| **2번째 고양이** | 추가 1x1 고양이 블록 자동 배치 |
| **클리어 조건** | 양쪽 고양이 모두 자신의 출구에 도달해야 클리어 |
| **등장 확률** | 30% (스테이지 131+) |

#### 2.1.12 클리어 조건

고양이 블록이 출구까지 이동하면 클리어됩니다. 추가 조건이 있는 경우 모두 충족해야 합니다:
- **열쇠-자물쇠**: 열쇠 블록이 자물쇠 셀 위에 있어야 함
- **체크포인트**: 고양이가 체크포인트 셀을 경유했어야 함
- **멀티캣**: 2마리 고양이 모두 각자의 출구에 도달해야 함

이동 횟수에 따라 별점이 부여됩니다:

| 별점 | 조건 |
|------|------|
| ★★★ | 최적 이동 횟수 이내 (moves ≤ optimal) |
| ★★ | 최적 × 1.5 이내 (moves ≤ optimal × 1.5) |
| ★ | 클리어만 하면 |

**별점 목표 HUD**: 플레이 중 HUD에 별 기준 이동 횟수를 미리 표시합니다 (예: `★★★ ≤5  ★★ ≤7  ★ 7+`). 현재 이동 횟수는 별 달성 가능 여부에 따라 색상이 변합니다:
- **녹색**: 3성 달성 가능
- **황색**: 2성 달성 가능
- **빨간색**: 1성

### 2.2 게임 플로우

```
┌──────────────────────────┐
│   스테이지 로딩           │
│   (퍼즐 생성 + BFS 솔버) │
└──────────────────────────┘
           ↓
┌──────────────────────────┐
│   PLAYING 상태            │
│   블록 드래그 → 스냅      │
│   Undo / Reset 가능      │
└──────────────────────────┘
           ↓
     고양이 탈출 조건?
      ↙        ↘
    NO          YES
     ↓            ↓
  계속 플레이  ┌──────────────────────────┐
              │   ESCAPING 상태           │
              │   Phase 1: 벽 열림 (200ms)│
              │   Phase 2: 슬라이드 (300ms)│
              │   Phase 3: 파티클 (500ms) │
              └──────────────────────────┘
                       ↓
              ┌──────────────────────────┐
              │   SOLVED (승리) 오버레이  │
              │   별점 + 이동 횟수 표시   │
              │   "Next Stage" 버튼       │
              └──────────────────────────┘
                       ↓
              ┌──────────────────────────┐
              │   Next Stage 클릭         │
              │   (조건 충족 시 전면 광고) │
              │   → 다음 스테이지 로드    │
              └──────────────────────────┘
```

### 2.3 조작 방식

| 조작 | 동작 |
|------|------|
| **블록 드래그** | 블록을 터치 후 이동 축 방향으로 드래그 |
| **고양이 이동** | 고양이는 블록 대신 캐릭터 이미지가 직접 이동 (드래그 시 1.05x 스케일업) |
| **스냅** | 드래그 종료 시 가장 가까운 칸으로 120ms ease-out 애니메이션 |
| **제스처 보호** | 보드 영역에서 시스템 뒤로가기 제스처 차단 (Android 10+, `systemGestureExclusionRects`) |
| **보드 중앙 고정** | 출구 방향과 무관하게 4방향 동일 여백으로 보드 항상 화면 중앙 배치 |
| **Undo** | 마지막 이동을 되돌림 |
| **Reset** | 퍼즐을 초기 상태로 리셋 |
| **일시정지** | HUD의 일시정지 버튼 → Resume / Restart / Quit |

---

## 3. 퍼즐 생성 시스템 (Puzzle Generation)

### 3.1 의존성 체인 빌더

1. **고양이 배치**: 출구 방향에 따라 고양이 블록 배치
2. **직접 차단**: 고양이 경로 위에 블록 배치 (직접 차단)
3. **간접 차단**: 직접 차단 블록의 이동 경로를 가로막는 블록 배치
4. **중첩 의존성**: 간접 차단 블록을 또 다른 블록이 차단 → 연쇄 의존
5. **BFS 검증**: 최소 이동 횟수가 난이도 임계값 이상인지 확인

### 3.2 BFS 솔버

- 상태를 IntArray(블록 위치 + 체크포인트 비트)로 표현 → PuzzleGrid clone() 없이 탐색
- FNV-1a 64비트 해시로 방문 체크 (충돌 확률 최소화)
- 최대 80,000~150,000 상태 (메커니즘 조합에 따라 동적), 깊이 35로 제한
- **벽 블록**: 이동 생성 시 자동 스킵
- **얼음 타일**: 얼음 셀에서 중간 정지 금지 — 벽/장애물에 부딪힐 때까지 슬라이딩
- **연결 블록**: linkId 파트너 맵으로 O(1) 조회, 쌍 동시 이동 + 충돌 검사
- **포탈**: 고양이 이동 후 목적지 점유 검사 + 자동 워프 (일반 블록 무시)
- **멀티캣**: 복수 고양이 인덱스 추적, `isCatAtExit()` 헬퍼로 각 고양이별 출구 검증
- `isSolvedState()`가 exitRow/exitCol + 열쇠 위치 + 체크포인트 도달 + 멀티캣 출구를 종합 검증
- 체크포인트 경유(pass-through) 감지: 이동 경로 중간에 체크포인트가 있으면 도달 처리 (연결 파트너 포함)

### 3.3 품질 필터 (Quality Filter)

`isQualityPuzzle()` 메서드가 생성된 퍼즐의 품질을 검사합니다:

| 검사 항목 | 조건 | 설명 |
|----------|------|------|
| **단일 방향** | 직접 차단 블록이 모두 같은 방향 | 다양성 부족 → 거부 |
| **단일 블록** | 직접 차단 블록이 1개 이하 (스테이지 > 5) | 너무 쉬움 → 거부 |
| **독립 이동** | 2개 이상 차단 블록이 모두 자유롭게 이동 가능 | 상호 의존성 없음 → 거부 |

- 품질 실패 시 `baseSeed + offset * 10000`으로 재생성 (최대 50회)
- `seedOffsetCache`에 오프셋 저장으로 동일 시드 재진입 시 결정적 결과 보장
- 스테이지 1~5는 입문용으로 단일 블록 검사 면제

---

## 4. 난이도 설계 (Difficulty Design)

### 4.1 구간별 난이도

| 구간 | 스테이지 | 격자 크기 | 블록 수 | 최소 이동 |
|------|---------|----------|--------|----------|
| **입문** | 1~5 | 5x5 | 3~5 | 2~6 |
| **초급** | 6~15 | 5x5 | 5~8 | 5~7 |
| **중급** | 16~30 | 6x6 | 6~10 | 8~10 |
| **중상급** | 31~50 | 6x6 | 8~12 | 10~12 |
| **상급** | 51~75 | 7x7 | 9~14 | 12~14 |
| **고급** | 76~100 | 7x7 | 10~15 | 15~16 |
| **마스터** | 101+ | 7x7 | 11~16 | 16 |

### 4.2 시드 기반 재현성

- 각 스테이지 번호가 시드로 사용 (`java.util.Random(stage)`)
- 동일 스테이지는 항상 동일한 퍼즐 생성
- 품질 필터 오프셋도 캐시되어 결정적 재진입 보장

---

## 5. 고양이 컬렉션 시스템 (Cat Collection)

### 5.1 고양이 목록

총 20마리의 고양이가 특정 스테이지 클리어 시 해금됩니다:

| # | 이름 | 해금 스테이지 | # | 이름 | 해금 스테이지 |
|---|------|-------------|---|------|-------------|
| 1 | 나비 | 1 | 11 | 가을 | 50 |
| 2 | 치즈 | 5 | 12 | 겨울 | 55 |
| 3 | 모모 | 10 | 13 | 솜이 | 60 |
| 4 | 구름 | 15 | 14 | 꽃이 | 65 |
| 5 | 호랑 | 20 | 15 | 하늘 | 70 |
| 6 | 까미 | 25 | 16 | 바다 | 75 |
| 7 | 별이 | 30 | 17 | 무지개 | 80 |
| 8 | 달이 | 35 | 18 | 보석 | 85 |
| 9 | 봄이 | 40 | 19 | 왕자 | 90 |
| 10 | 여름 | 45 | 20 | 공주 | 95 |

### 5.2 컬렉션 화면

- **4열 그리드** 형태로 20마리 표시
- **해금된 고양이**: 컬러 이미지 + 이름 표시, 탭하여 선택 가능
- **잠긴 고양이**: 그레이스케일 + "Stage XX 클리어 시 해금" 텍스트
- **선택된 고양이**: 코랄색 테두리 + "SELECTED" 배지

### 5.3 게임 연동

- 선택한 고양이가 퍼즐 화면의 고양이 블록에 표시됨
- 스테이지 최초 클리어 시 새 고양이 해금 → 축하 다이얼로그 표시
- 선택 상태는 SharedPreferences에 영구 저장

---

## 6. 애니메이션 시스템 (Animation)

### 6.1 블록 스냅 애니메이션

| 항목 | 값 |
|------|-----|
| 지속 시간 | 120ms |
| 보간 함수 | Ease-out Quadratic: `1 - (1-t)²` |
| 효과음 | `playBlockMatch()` |
| 입력 차단 | `snapAnimating` 플래그로 애니메이션 중 입력 무시 |

### 6.2 고양이 탈출 시퀀스

클리어 조건 달성 시 3단계 연출이 진행됩니다:

| Phase | 내용 | 지속 시간 | 효과음 |
|-------|------|----------|--------|
| **1: 벽 열림** | 출구 벽이 양쪽으로 갈라짐 | 200ms | - |
| **2: 슬라이드** | 고양이가 출구 방향으로 미끄러져 나감 | 300ms (ease-in: `t²`) | `playCatRescue()` |
| **3: 파티클** | 파스텔 색상 파티클 폭발 효과 | 500ms | `playLevelClear()` |

### 6.3 파티클 시스템

- 30개의 원형 파티클 생성 (`Canvas.drawCircle`)
- 랜덤 크기(3~8dp), 속도, 방향
- 중력 가속도 적용 (매 프레임 vy += 0.3)
- 알파값 감소로 페이드아웃
- 6가지 파스텔 컬러: 핑크, 블루, 그린, 옐로우, 퍼플, 오렌지

---

## 7. 수익화 전략 (Monetization)

### 7.1 광고 정책

| 항목 | 값 |
|------|-----|
| **광고 미노출 구간** | 스테이지 1~5 (신규 유저 보호) |
| **전면 광고 간격** | 3 스테이지마다 |
| **세션당 최대 광고** | 10회 |
| **전면 광고 타이밍** | "Next Stage" 버튼 클릭 시 (클리어 직후가 아님) |

### 7.2 광고 유형

| 광고 유형 | 위치 | 설명 |
|-----------|------|------|
| **배너 광고** | 게임 화면 하단 | 상시 표시 |
| **전면 광고** | Next Stage 클릭 시 | 3스테이지 간격, 스테이지 5 이후 |
| **보상형 광고** | 힌트/부활 | 사용자 선택적 (강제 시청 없음) |

### 7.3 광고 플로우

```
스테이지 클리어 → 탈출 애니메이션 → 승리 오버레이 표시
  → 사용자가 "Next Stage" 버튼 클릭
    → shouldShowInterstitial() 체크
      → 조건 충족: 전면 광고 표시 → 광고 닫힘 → 다음 스테이지 로드
      → 조건 미충족: 바로 다음 스테이지 로드
```

---

## 8. 기술 구현 (Technical Implementation)

### 8.1 기술 스택

| 구분 | 기술 |
|------|------|
| **언어** | Kotlin 2.2.0 |
| **렌더링** | Android SurfaceView + Canvas (60fps) |
| **데이터 저장** | Room 2.6.1 DB + SharedPreferences |
| **광고** | Google AdMob SDK |
| **빌드** | Gradle 9.0.0 + AGP 8.7.3 |
| **최소 SDK** | API 24 (Android 7.0) |
| **타겟 SDK** | API 35 (Android 15) |

### 8.2 패키지 구조

```
com.meowrescue.game
├── MeowRescueApp.kt                    // 앱 초기화 (AdMob 등)
├── puzzle/
│   ├── PuzzleGrid.kt                   // 격자 상태, 블록 이동, 클리어 판정
│   │                                   // ExitDirection, MoveRecord, BFS 솔버
│   └── PuzzleGenerator.kt             // 자동 레벨 생성 + 품질 필터
│                                       // seedOffsetCache, isQualityPuzzle()
├── ui/
│   ├── MenuActivity.kt                 // 메인 메뉴 (Play, Collection, Sound)
│   ├── StageSelectActivity.kt          // 스테이지 선택 (4열 그리드, 별점)
│   ├── PuzzleActivity.kt              // 게임 진행, 클리어 처리, 고양이 해금
│   ├── PuzzleView.kt                  // SurfaceView 렌더링 + 드래그 + 애니메이션
│   │                                   // 스냅, 탈출 시퀀스, 파티클, 승리 오버레이
│   ├── CollectionActivity.kt          // 고양이 컬렉션 (4열 그리드, 선택/해금)
│   └── Theme.kt                        // UI 색상 + 파티클 컬러
├── data/
│   ├── AppDatabase.kt                  // Room 데이터베이스
│   ├── UserProgressDao.kt             // 진행도 DAO
│   └── GameRepository.kt              // 데이터 접근 + 고양이 컬렉션 관리
│                                       // CatDefinition, 20마리 정의
├── ads/
│   └── AdManager.kt                    // AdMob 광고 관리
│                                       // 전면 광고 간격 카운터 시스템
└── util/
    └── SoundManager.kt                 // 효과음 + BGM
```

### 8.3 핵심 데이터 구조

```kotlin
// ── 퍼즐 블록 ──
data class PuzzleBlock(
    val id: Int, val row: Int, val col: Int,
    val length: Int, val isHorizontal: Boolean,
    val isCat: Boolean = false,
    val isKey: Boolean = false,         // 열쇠 블록 여부
    val isWall: Boolean = false,        // 벽 블록 (이동 불가)
    val linkId: Int = -1                // 연결 블록 쌍 ID (-1=없음)
)

// ── 퍼즐 격자 ──
class PuzzleGrid(
    val rows: Int, val cols: Int,
    val exitRow: Int, val exitCol: Int,
    val exitDirection: ExitDirection,
    val hasKeyLock: Boolean = false,     // 열쇠-자물쇠 메커니즘 활성화
    val lockRow: Int = -1,              // 자물쇠 셀 행
    val lockCol: Int = -1,              // 자물쇠 셀 열
    val checkpointRow: Int = -1,        // 체크포인트 셀 행
    val checkpointCol: Int = -1,        // 체크포인트 셀 열
    val iceCells: Set<Int> = emptySet(),// 얼음 타일 (row*cols+col 인코딩)
    val portalA: Int = -1,              // 포탈 A 위치 (row*cols+col, -1=없음)
    val portalB: Int = -1,              // 포탈 B 위치
    val exitRow2: Int = -1,             // 멀티캣 2번째 출구 행
    val exitCol2: Int = -1,             // 멀티캣 2번째 출구 열
    val exitDirection2: ExitDirection? = null // 멀티캣 2번째 출구 방향
)

// ── 출구 방향 ──
enum class ExitDirection { RIGHT, LEFT, TOP, BOTTOM }

// ── 이동 기록 ──
data class MoveRecord(
    val blockId: Int, val steps: Int,
    val horizontal: Boolean,
    val setCheckpoint: Boolean = false,  // 이 이동이 체크포인트를 활성화했는지 (undo 복원용)
    val partnerId: Int = -1,            // 연결 블록 파트너 ID
    val didPortal: Boolean = false,     // 포탈 텔레포트 발동 여부
    val portalFromRow: Int = -1,        // 포탈 워프 전 행
    val portalFromCol: Int = -1         // 포탈 워프 전 열
)

// ── 스테이지 피처 ──
data class StageFeatures(
    val hasKey: Boolean,                // 열쇠-자물쇠 활성화
    val hasCheckpoint: Boolean,         // 체크포인트 활성화
    val hasWalls: Boolean = false,      // 벽 블록
    val hasIce: Boolean = false,        // 얼음 타일
    val hasLinkedBlocks: Boolean = false,// 연결 블록
    val hasPortals: Boolean = false,    // 포탈
    val hasMultiCat: Boolean = false    // 멀티캣
)

// ── 고양이 정의 ──
data class CatDefinition(
    val id: Int, val name: String,
    val requiredStage: Int, val drawableRes: Int
)
```

### 8.4 게임 상태

```kotlin
enum class PuzzleState {
    PLAYING,    // 블록 이동 가능
    ESCAPING,   // 탈출 애니메이션 진행 중
    SOLVED      // 승리 오버레이 표시
}
```

### 8.5 저장 시스템

| 저장소 | 용도 |
|--------|------|
| **Room DB** | 스테이지 클리어 상태, 별점, 최고 기록 |
| **SharedPreferences** | 선택된 고양이 ID, 사운드 설정 |

---

## 9. UI/UX 설계

### 9.1 화면 구성

| 화면 | 주요 요소 |
|------|-----------|
| **메인 메뉴** | Meow Rescue 로고, Play, Collection, Sound 토글 |
| **스테이지 선택** | 4열 그리드, 별점 표시, 잠금/해금, 스크롤 |
| **퍼즐 화면** | 격자 + 블록 + HUD(Pause/Undo/Reset/이동수) + 배너 광고 |
| **승리 오버레이** | 별점 + 이동 횟수 + "Next Stage" 버튼 |
| **일시정지** | Resume / Restart / Quit 버튼 |
| **컬렉션** | 4열 고양이 그리드, 선택/해금 상태 |
| **축하 다이얼로그** | 새 고양이 해금 시 이미지 + 이름 표시 |
| **로딩 화면** | 고양이 이미지 + "Preparing puzzle..." 텍스트 |

### 9.2 디자인 톤

- **분위기**: 밝고 따뜻한 파스텔 톤
- **배경색**: 크림 (#FFF8F0)
- **격자 배경**: 연한 브라운 (#D7CCC8)
- **블록 색상**: 파스텔 팔레트 (7가지 색상)
- **고양이 블록**: 코랄 (#FF7043)
- **출구 표시**: 연한 초록 (#C8E6C9)
- **사운드**: 경쾌한 블록 이동음, 고양이 구출 효과음, 레벨 클리어음, BGM

---

## 10. 테스트

### 10.1 단위 테스트 (JUnit 4)

| 테스트 | 검증 내용 |
|--------|-----------|
| `stage82IsSolvable` | 스테이지 82가 풀이 가능한지 검증 |
| `allDirectionsAppear` | 100개 스테이지에서 4방향 출구 모두 등장 |
| `oneCellBlocksMoveBothAxes` | 1칸 블록이 상하좌우 모두 이동 가능 |
| `multiCellBlocksRestrictedToAxis` | 다칸 블록이 축 방향으로만 이동 |
| `sampleStagesSolvable` | 1~100 스테이지 모두 풀이 가능 |
| `oneCellCatSolvesAtExit` | 1x1 고양이 출구 도달 시 클리어 |
| `oneCellCatWrongRowNotSolved` | 잘못된 행에서 미클리어 |
| `keyLockRequiresKeyAtLock` | 열쇠 미배치 시 미클리어 / 배치 시 클리어 |
| `checkpointMustBeReached` | 체크포인트 미경유 시 미클리어 / 경유 시 클리어 |
| `checkpointReachedByMoving` | 이동으로 체크포인트 도달 감지 |
| `undoRevertsCheckpoint` | Undo 시 체크포인트 상태 되돌림 |
| `checkpointPassThrough` | 슬라이드 중간 체크포인트 경유 감지 |
| `keyAndCheckpointStagesSolvable` | 열쇠/체크포인트 스테이지 풀이 가능성 |
| `featuresForStageDistribution` | 스테이지별 피처 분포 정확성 (벽/얼음/연결/포탈/멀티캣 포함) |
| `wallBlocksImmovable` | 벽 블록 이동 불가 검증 |
| `linkedBlocksMoveTogether` | 연결 블록 동시 이동 검증 |
| `linkedBlockUndoRestoresBoth` | 연결 블록 Undo 시 양쪽 복원 |
| `portalTeleportsCat` | 포탈 A→B 텔레포트 검증 |
| `portalUndoRestoresPosition` | 포탈 Undo 시 원래 위치 복원 |
| `multiCatBothMustExit` | 멀티캣 양쪽 출구 도달 필수 검증 |
| `newFeaturesStagesSolvable` | 51~140 스테이지 신규 메커니즘 풀이 가능성 |
| `debugStageInfo` | 주요 스테이지 디버그 정보 출력 |

---

*Meow Rescue GDD v5.0 — 2026.03*
*Platform: Android (Kotlin) | Genre: Sliding Block Puzzle | Style: Casual*
