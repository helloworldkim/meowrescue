/**
 * All types in this package are `internal` by ADR-0006 § Layer Boundary Enforcement.
 * Do not add `public` types here.
 * The sole `public` export from `data/` is `GameRepository`.
 */
package com.meowrescue.game.data
